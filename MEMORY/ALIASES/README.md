# ALIASES

Status: SCAFFOLD_ONLY

Scope: Alias and mirror records that point to canonical records.

Rules:
- Alias files are navigation only.
- Use `do_not_edit_here=YES` for mirrors.
- Never create a second truth.
